"""
Constants shared across the hamer package.
Derived from HaWoR's lib/core/constants.py — only the symbols needed
by TrackDatasetEval and imutils are kept here.
"""

# Mean and standard deviation for normalizing input image (ImageNet defaults)
IMG_NORM_MEAN = [0.485, 0.456, 0.406]
IMG_NORM_STD = [0.229, 0.224, 0.225]
